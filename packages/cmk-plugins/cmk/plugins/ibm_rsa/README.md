# IBM RSA

Monitoring for the **IBM Remote Supervisor Adapter (RSA II)**, the
out-of-band management adapter used in older IBM System x / eServer xSeries
servers (the predecessor of the IMM).

The check plugin polls the RSA over SNMP to monitor the overall hardware
health status reported by the adapter.
