# IBM Storage TS

Monitoring for **IBM System Storage TS-series tape libraries** (e.g. the
TS3100/TS3200/TS3500 family).

The check plugins poll the library over SNMP to monitor the device status,
the tape library subsystem, and the individual tape drives.
