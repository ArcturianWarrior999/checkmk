# IBM ServeRAID (xraid)

Monitoring for **IBM ServeRAID** controllers in IBM xSeries / System x
servers.

The check plugin polls the ServeRAID agent over SNMP to monitor the state of
the physical disks attached to the RAID controller.
