# IBM Informix

Monitoring for **IBM Informix** database servers.

An agent plugin runs `onstat`/SQL queries against each configured Informix
instance. The check plugins monitor dbspaces (capacity), logical-log usage,
locks, active sessions, table extents and the overall server status.
